// Версия плагина. В CI файл перезаписывается из тега перед сборкой
// (см. .github/workflows/release.yml): версия обязана иметь ровно один источник,
// и этот источник — тег. Значение ниже — фолбэк для локальных сборок.
#pragma once

#ifndef CH_VERSION
#define CH_VERSION "0.0.0-dev"
#endif
