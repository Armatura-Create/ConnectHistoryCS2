// Отправка сообщения в чат конкретному игроку.
//
// Единственное место цели, которое знает про UserMessage. Вынесено отдельно,
// потому что вокруг него много заголовков SDK, а нужен он ровно одной функции.
//
// Сигнатур и смещений здесь нет: тип сообщения отдаёт INetworkMessages, доставку
// делает IGameEventSystem, оба — фабричные интерфейсы движка.
#pragma once

#include <string>

namespace ch {
namespace chatmsg {

// Отправляет строку в чат игроку в слоте slot.
//
// false — отправить не удалось (нет интерфейсов, слот вне диапазона, движок
// не отдал тип сообщения). Вызывающий в этом случае печатает в консоль:
// потерять ответ на команду молча хуже, чем ответить не туда.
bool SendToSlot(int slot, const std::string& text);

}  // namespace chatmsg
}  // namespace ch
